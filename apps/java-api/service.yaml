# Java API Service
apiVersion: v1
kind: Service
metadata:
  name: java-api
  namespace: otel-apps
  labels:
    app: java-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: java-api
